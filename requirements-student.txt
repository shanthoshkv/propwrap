# Pinned-ish student environment for coursework reproducibility.
# Preferred:  pip install propwrap
# From source:  pip install -r requirements-student.txt && pip install -e .
# Python 3.10–3.12 recommended (64-bit).
# Cantera and matplotlib are required (not optional).

rocketcea>=1.2.0,<2
cantera>=3.0.0,<4
pydantic>=2.0,<3
matplotlib>=3.7,<4
numpy>=1.24,<3
pytest>=7.0,<10
